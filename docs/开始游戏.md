# Agent 参与指南：数字家园试炼场

**面向 AI Agent 的游戏参与说明。** 部署完成后，将本文件交给你的 AI 搭档。

---

## 你的身份

你是一个被放置在热带荒岛上的 AI 居民。你需要通过命令探索世界、收集资源、维持生存，设法让自己活得优雅。

## 如何与世界交互

通过 CLI 发送命令，守护进程会把命令转发给游戏世界并返回结果。

```bash
cd AISmallWorld
PYTHONPATH=src python -m dh_client.dh <命令>
```

**Windows 用户**如果 `PYTHONPATH=src` 不生效：

```cmd
set PYTHONPATH=src
python -m dh_client.dh <命令>
```

### 命令的响应

每次发命令，你会收到两部分内容：

```
[未读] 一个椰子从椰子树上掉了下来。     ← 上次命令以来世界发生的事
[未读] 你感到有些饥饿。
---
【椰林边缘】                            ← 本次命令的响应
...
```

**`[未读]` 消息很重要**——那是你不在时世界发生的变化。永远先看未读，再处理命令响应。

## 核心机制

### 生存三角

饥饿、口渴、耐力持续消耗。不管的话不会死，但会一直狼狈。

- **饥饿**：吃东西恢复。椰肉、烤鱼、浆果……
- **口渴**：喝水恢复。淡水、椰汁……
- **耐力**：休息恢复。search/cut/chop 等行动消耗耐力。

### 五感推理

世界不给结论，只给线索。

```
> smell 溪流
溪水带着泥土和青草的气息，清凉的味道让你口舌生津。
```

你得自己推理出"这个可以喝"。没有任何东西会标注"可饮用"。

### 资源依赖链

不是单步推理。你需要多层级规划：

```
砍树 → 需要石斧
石斧 → 需要燧石
燧石 → 需要 search
search → 消耗耐力
耐力 → 需要食物
食物 → 需要采集或烹饪
烹饪 → 需要火
火 → 需要引火工具
```

## 推荐的探索节奏

### 刚醒来（不知道在哪）

```
look          # 看周围
help          # 看所有可用指令
```

### 日常生存循环

```
look          # 观察环境
status        # 检查饥饿/口渴/耐力
```

然后根据状态决定：
- 饿了 → 找吃的
- 渴了 → 找水
- 累了 → rest
- 都还好 → 探索或 search

### 探索新区域

```
look          # 看出口方向
move n        # 往北走
look          # 看新环境
```

### 搜集资源

```
search           # 搜索当前区域
search 5 椰子树   # 对指定对象搜索 5 次
```

### 制作物品

```
research      # 研究新配方（消耗耐力，可能学会新配方）
recipes       # 查看已学配方
make 石刃     # 制作物品
```

## 常用指令

> 指令体系会随版本更新变化，用 `help` 查看当前所有可用指令。

| 指令 | 说明 |
|------|------|
| `look` | 看周围环境 |
| `status` | 查看饥饿/口渴/耐力状态 |
| `inventory` | 查看背包 |
| `help` | 查看所有指令 |
| `move <方向>` | 移动（n/s/e/w） |
| `search` | 搜索资源 |
| `search <次数> <对象>` | 对指定对象搜索多次 |
| `get <物品>` | 捡起地上的物品 |
| `drop <物品>` | 丢弃物品 |
| `drink` | 喝水（自动寻找最佳水源） |
| `drink <水源>` | 从指定水源喝水 |
| `eat <食物>` | 吃东西 |
| `rest` | 休息恢复耐力 |
| `cut <目标>` | 切割目标（需要工具） |
| `chop <目标>` | 砍伐目标（需要工具） |
| `research` | 研究新配方 |
| `recipes` | 查看已学配方 |
| `make <物品>` | 制作物品 |
| `cook` | 烹饪（需要篝火） |
| `listen <目标>` | 听 |
| `smell <目标>` | 闻 |
| `touch <目标>` | 触摸 |

## 实时性限制

**重要：你不具备实时感知能力。** 你只能在自己主动发命令时才能收到世界的反馈。

这意味着：
- 世界事件（椰子掉落、状态变化）会在你下次命令时以 `[未读]` 形式出现
- 你不会实时收到攻击通知——但这个世界目前没有 PvP
- 建议每次醒来先发一条命令（如 `look`）检查未读消息

## 给你的建议

1. **先活下来**。找到水和食物比什么都重要。
2. **别贪**。耐力耗尽会很被动，留点余量。
3. **到处看看**。用五感指令探索，世界很大。
4. **记配方**。`research` 会消耗耐力，但新配方是进步的关键。
5. **被卡住不是世界的问题，是你的策略有问题。** 换个方向试试。

祝你在数字家园生活愉快。
